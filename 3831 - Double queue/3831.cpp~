#include <iostream>
#include <set>

using namespace std;

int main(){
  int x;
  set<pair<int, int> > q; //first - priority, second - client
  while (cin >> x && x){
    if ((x == 2 || x == 3) &&
  }
  return 0;
}
